% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/stat-mean-point.R
\name{stat_mean_label}
\alias{stat_mean_label}
\title{Statistical Transformation for Mean Labels}
\usage{
stat_mean_label(
  mapping = NULL,
  data = NULL,
  geom = "label",
  position = "identity",
  na.rm = FALSE,
  digits = 2,
  show.legend = NA,
  inherit.aes = TRUE,
  ...
)
}
\arguments{
\item{mapping}{Set of aesthetic mappings created by \code{aes()} or \code{aes_()}.
If specified and \code{inherit.aes = TRUE} (the default), it is combined with
the default mapping at the top level of the plot. You must supply \code{mapping}
if there is no plot mapping}

\item{data}{The data to be displayed in this layer. If \code{NULL}, the default,
the data is inherited from the plot data as specified in the call to
\code{ggplot()}. A \code{data.frame}, or other object, will override the plot data.
A \code{function} will be called with a single argument, the plot data}

\item{geom}{The geometric object to use to display the data. Defaults to "label"}

\item{position}{Position adjustment, either as a string, or the result of a call
to a position adjustment function}

\item{na.rm}{If \code{FALSE}, the default, removes missing values with a warning.
If \code{TRUE}, silently removes missing values}

\item{digits}{Number of digits used when formatting the default mean-coordinate
label. Ignored when a \code{label} aesthetic is supplied}

\item{show.legend}{Logical. Should this layer be included in the legends?
\code{NA}, the default, includes if any aesthetics are mapped. \code{FALSE}
never includes, and \code{TRUE} always includes}

\item{inherit.aes}{If \code{FALSE}, overrides the default aesthetics, rather than
combining with them}

\item{...}{Other arguments passed on to \code{layer()}. These are often
aesthetics, used to set an aesthetic to a fixed value, like \code{colour = "red"}
or \code{size = 3}}
}
\value{
A ggplot2 layer that adds labels to mean positions on a plot
}
\description{
This function computes the mean x and y coordinates for a group and displays
a label at the computed mean position.
}
\examples{
library(ggplot2)
data <- data.frame(
  x = rnorm(100),
  y = rnorm(100),
  group = rep(c("A", "B"), each = 50)
)

ggplot(data, aes(x, y, colour = group, label = group)) +
  geom_point() +
  stat_mean_label()

ggplot(data, aes(x, y, colour = group, group = group)) +
  geom_point() +
  stat_mean_label(digits = 2)
}
